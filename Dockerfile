# Dockerfile — cri-odoo-custom:v1
#
# A minimal Dockerfile extending the official Odoo 17 image.
# The report confirms this image was built, exported as cri-odoo-custom.tar.gz,
# and loaded on the Ubuntu VM for offline deployment.
#
# The specific customisation content was not reproduced in the report.
# Fill in any custom modules, configurations, or entrypoint overrides below.

FROM odoo:17

# يرجى ملء هذا الملف حسب التقرير (لم يتم العثور على محتوى محدد)
# Add any custom Odoo modules, pip packages, or config overrides here.
# Example:
# COPY ./custom_modules /mnt/extra-addons
# COPY ./odoo.conf /etc/odoo/odoo.conf
